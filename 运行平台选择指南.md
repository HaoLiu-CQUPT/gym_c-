# 运行平台选择指南

本项目是一个 **Qt Widgets 桌面应用程序**，需要图形界面支持。以下是可用的运行平台：

## 🌐 在线平台（推荐）

### 1. **GitHub Codespaces** ⭐ 推荐
- **优点**：免费额度充足，稳定，与GitHub集成好
- **访问方式**：
  1. 访问：https://github.com/HaoLiu-CQUPT/gym_c-
  2. 点击绿色的 "Code" 按钮
  3. 选择 "Codespaces" → "Create codespace on main"
- **配置**：需要创建 `.devcontainer` 配置文件（见下方）

### 2. **Gitpod** 
- **优点**：专为开发环境设计，配置完善
- **访问方式**：https://gitpod.io/#https://github.com/HaoLiu-CQUPT/gym_c-
- **注意**：免费版有使用时间限制

### 3. **Replit**
- **优点**：简单易用，支持图形界面
- **访问方式**：
  1. 注册 Replit 账号：https://replit.com
  2. 导入 GitHub 仓库
  3. 选择 C++ 模板
- **限制**：需要手动配置 Qt

## 💻 本地运行（最稳定）

### Windows
```bash
# 方法1：使用 vcpkg
# 1. 安装 vcpkg
git clone https://github.com/Microsoft/vcpkg.git
cd vcpkg
.\bootstrap-vcpkg.bat

# 2. 安装 Qt
.\vcpkg.exe install qt5-base:x64-windows

# 3. 编译项目
cd 项目目录
mkdir build && cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=路径/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build .
```

### Linux (Ubuntu/Debian)
```bash
# 1. 安装 Qt 和编译工具
sudo apt-get update
sudo apt-get install qt6-base-dev qt6-base-dev-tools cmake build-essential

# 2. 编译项目
mkdir build && cd build
cmake ..
make

# 3. 运行
./GymManagementSystem
```

### macOS
```bash
# 1. 安装 Qt（使用 Homebrew）
brew install qt@6

# 2. 编译项目
mkdir build && cd build
cmake .. -DCMAKE_PREFIX_PATH=$(brew --prefix qt@6)
make

# 3. 运行
./GymManagementSystem
```

## 🔧 快速解决方案

### 方案A：使用 GitHub Codespaces（最简单）

1. **创建 Codespaces 配置**：
   - 项目已包含 `.gitpod.yml`，可以创建类似的 Codespaces 配置

2. **访问项目**：
   - 打开：https://github.com/HaoLiu-CQUPT/gym_c-
   - 点击 "Code" → "Codespaces" → "New codespace"

### 方案B：本地安装 Qt（最稳定）

1. **下载 Qt**：
   - 访问：https://www.qt.io/download
   - 下载 Qt Online Installer
   - 安装 Qt 6.x 或 Qt 5.x（选择 MinGW 或 MSVC 编译器）

2. **配置环境变量**：
   ```bash
   # Windows: 将 Qt 的 bin 目录添加到 PATH
   # 例如：C:\Qt\6.5.0\mingw_64\bin
   ```

3. **编译运行**：
   ```bash
   mkdir build && cd build
   cmake ..
   cmake --build .
   # Windows: .\GymManagementSystem.exe
   # Linux/Mac: ./GymManagementSystem
   ```

## 📝 各平台对比

| 平台 | 免费额度 | 图形界面 | 配置难度 | 推荐度 |
|------|----------|----------|----------|--------|
| GitHub Codespaces | 60小时/月 | ✅ 支持 | ⭐⭐ 中等 | ⭐⭐⭐⭐⭐ |
| Gitpod | 50小时/月 | ✅ 支持 | ⭐ 简单 | ⭐⭐⭐⭐ |
| Replit | 有限 | ✅ 支持 | ⭐⭐⭐ 较难 | ⭐⭐⭐ |
| 本地运行 | 无限制 | ✅ 支持 | ⭐⭐ 中等 | ⭐⭐⭐⭐⭐ |

## 🚀 推荐方案

**如果你想要最简单的方式**：
1. 使用 **GitHub Codespaces**（免费，配置简单）
2. 或 **本地安装 Qt**（最稳定，无时间限制）

**如果你想要在线开发**：
1. 优先使用 **GitHub Codespaces**
2. 备选 **Gitpod**

## ❓ 常见问题

### Q: 为什么 Qt 平台运行不了？
A: 可能是：
- 网络连接问题
- Qt 版本不兼容
- 缺少依赖库
- 图形界面不支持

### Q: 哪个平台最稳定？
A: **本地运行**最稳定，但需要安装 Qt。在线平台中，**GitHub Codespaces** 最稳定。

### Q: 免费额度用完了怎么办？
A: 可以：
- 切换到其他平台
- 本地安装 Qt 运行
- 等待下个月额度重置

## 📞 需要帮助？

如果遇到问题，可以：
1. 检查项目的 `.gitpod.yml` 配置文件
2. 查看 `README_Gitpod.md` 详细说明
3. 尝试本地编译运行

